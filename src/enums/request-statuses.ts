export enum RequestStatusesEnum {
    NEW = 'new',
    READ = 'read',
    DECLINED = 'declined',
    CANCELED = 'canceled',
}

export enum OfferToRequestStatusesEnum {
    NEW = 'new',
    ACCEPTED = 'accepted',
    DECLINED = 'declined',
    CANCELED = 'canceled',
}
