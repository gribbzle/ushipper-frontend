export enum OrderStatus {
    NEW = 'new',
    ON_HOLD = 'on_hold',
    PENDING = 'pending',
    DECLINED = 'declined',
    ACCEPTED = 'accepted',
    PICKED_UP = 'picked_up',
    DELIVERED = 'delivered',
    POSTED = 'posted',
    CANCELED = 'cancelled',
}
