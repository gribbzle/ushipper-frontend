export enum FeeCategoryType {
    FACTORING = 'factoring',
    USHIPPER = 'ushipper',
    DISPATCH = 'dispatch',
    RECURRING = 'recurring',
    TO_WALLET = 'to_wallet',
    ORDER_INTERVAL_RECURRING = 'order_interval_recurring',
}
