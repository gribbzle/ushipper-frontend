export enum OrderStatisticsGroup {
    AGING_ORDERS = 'aging_orders',
    DELAYED_PICK_UPS = 'delayed_pick_ups',
    DELAYED_DELIVERIES = 'delayed_deliveries',
    TO_PICK_UP_TODAY = 'to_pick_up_today',
    TO_DELIVER_TODAY = 'to_deliver_today',
}
